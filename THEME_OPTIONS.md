# 🎨 Theme Options for Your Booking System

I've created 4 different color schemes for you to choose from. To switch themes, simply change the imports and colors in your App.tsx file.

---

## 🌊 Theme 1: Ocean Blue (Cool & Professional)
**Colors:** Cyan, Teal, Blue, Sky
**Vibe:** Clean, professional, trustworthy

### To activate:
1. Change line 13 in `/src/app/App.tsx`:
```tsx
import { AnimatedBackgroundOcean } from "@/app/components/animated-background-ocean";
```

2. Replace `<AnimatedBackground />` with:
```tsx
<AnimatedBackgroundOcean />
```

3. Update primary colors throughout:
- Replace `purple-` with `cyan-`
- Replace `pink-` with `teal-`
- Replace `blue-` with `sky-`

**Best for:** Tech companies, professional services, modern businesses

---

## 🌅 Theme 2: Sunset Warmth (Bold & Energetic)
**Colors:** Orange, Rose, Amber, Pink
**Vibe:** Warm, energetic, creative

### To activate:
1. Change line 13 in `/src/app/App.tsx`:
```tsx
import { AnimatedBackgroundSunset } from "@/app/components/animated-background-sunset";
```

2. Replace `<AnimatedBackground />` with:
```tsx
<AnimatedBackgroundSunset />
```

3. Update primary colors throughout:
- Replace `purple-` with `orange-`
- Replace `pink-` with `rose-`
- Replace `blue-` with `amber-`

**Best for:** Creative agencies, entertainment, lifestyle brands

---

## 💚 Theme 3: Emerald Professional (Fresh & Modern)
**Colors:** Emerald, Teal, Green
**Vibe:** Fresh, growth-oriented, professional

### To activate:
1. Change line 13 in `/src/app/App.tsx`:
```tsx
import { AnimatedBackgroundEmerald } from "@/app/components/animated-background-emerald";
```

2. Replace `<AnimatedBackground />` with:
```tsx
<AnimatedBackgroundEmerald />
```

3. Update primary colors throughout:
- Replace `purple-` with `emerald-`
- Replace `pink-` with `teal-`
- Replace `blue-` with `green-`

**Best for:** Eco-friendly businesses, health tech, financial services

---

## ⚫ Theme 4: Minimal Dark (Sleek & Sophisticated)
**Colors:** Indigo, Violet, Blue, Slate
**Vibe:** Minimal, sophisticated, modern

### To activate:
1. Change line 13 in `/src/app/App.tsx`:
```tsx
import { AnimatedBackgroundMinimal } from "@/app/components/animated-background-minimal";
```

2. Replace `<AnimatedBackground />` with:
```tsx
<AnimatedBackgroundMinimal />
```

3. Update primary colors throughout:
- Replace `purple-` with `indigo-`
- Replace `pink-` with `violet-`
- Keep `blue-` as is

**Best for:** SaaS platforms, developer tools, premium services

---

## 🚀 Quick Switch Instructions

Would you like me to implement one of these themes for you? Just tell me which number (1-4) and I'll update all the necessary files!

**Current theme:** Purple/Pink/Blue (Original)
